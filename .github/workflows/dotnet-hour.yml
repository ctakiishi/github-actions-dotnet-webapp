name: Hourly Job

on:
  schedule:
    - cron: '10 * * * *'  # Every hour at minute 0

jobs:
  run-hourly-task:
    name: Run Hourly Task
    runs-on: ubuntu-latest

    steps:
      - name: Print date and time
        run: date